{
  "name": "dindin-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "validate:env": "bun scripts/validate-env.ts",
    "setup:env": "cp apps/server/.env.example apps/server/.env && cp apps/native/.env.example apps/native/.env && echo '✅ Environment files created. Please configure them and run: bun run validate:env'"
  },
  "dependencies": {
    "use-debounce": "^10.0.0",
    "expo-linear-gradient": "^13.0.2"
  },
  "devDependencies": {
    "@testing-library/react-native": "^12.4.3",
    "@testing-library/jest-native": "^5.4.3",
    "jest": "^29.7.0",
    "@types/jest": "^29.5.8"
  }
}